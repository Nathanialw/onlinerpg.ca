'use strict'

let item = {
    iconPath: 0,
    durability: 0,
    rarity: 0,
    modStringsUIDArray: [],
    modStrings: [] //add teh mods that are like to display as consolidated values ie. if there are 2 + fire damage mods, add them together and save the string here to display
}

let inventory = {
    bag0: [], //array of items
    bag1: [],
    bag2: [],
    bag3: [],
    bag4: []    
};


//takes in a  string of data and returns a structure of the data
function ParseInventory(dataStr) {





    return remainingStr;
}